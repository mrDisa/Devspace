import { useNavigate } from 'react-router-dom'; import { Card } from '../shared/ui'; import { CommunityForm } from '../features/communities/ui/CommunityForm'; import { communitiesApi } from '../features/communities/api/communitiesApi'; import { routes } from '../shared/config/routes';
export function CommunityCreatePage() { const navigate = useNavigate(); const create = async (data) => { const { data:community } = await communitiesApi.create(data); navigate(routes.community(community.slug)); }; return <><header className="page-heading"><div><p className="eyebrow">НОВОЕ ПРОСТРАНСТВО</p><h1>Создать сообщество</h1><p>Объедините людей вокруг общей темы.</p></div></header><Card className="form-card"><CommunityForm submitLabel="Создать сообщество" onSubmit={create}/></Card></>; }
