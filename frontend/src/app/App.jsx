import { Navigate, Route, Routes } from 'react-router-dom';
import { AuthProvider, useAuth } from '../features/auth/model/AuthContext';
import { AppLayout } from '../widgets/AppLayout';
import { ErrorBoundary } from '../shared/ui/ErrorBoundary';
import { FeedPage } from '../pages/FeedPage'; import { ProfilePage } from '../pages/ProfilePage'; import { ProfileEditPage } from '../pages/ProfileEditPage'; import { NotificationsPage } from '../pages/NotificationsPage'; import { AuthPage } from '../pages/AuthPage';
import { CommunitiesPage } from '../pages/CommunitiesPage'; import { CommunityPage } from '../pages/CommunityPage'; import { CommunityCreatePage } from '../pages/CommunityCreatePage'; import { CommunityManagePage } from '../pages/CommunityManagePage';
function PrivateRoute({ children }) { const { user, loading } = useAuth(); if (loading) return <div className="app-loading">Загрузка Devspace…</div>; return user ? children : <Navigate to="/login" replace/>; }
function Protected({ children }) { return <PrivateRoute><AppLayout><ErrorBoundary>{children}</ErrorBoundary></AppLayout></PrivateRoute>; }
export function App() { return <AuthProvider><Routes><Route path="/login" element={<AuthPage/>}/><Route path="/signup" element={<AuthPage register/>}/><Route path="/feed" element={<Protected><FeedPage/></Protected>}/><Route path="/profile/:username" element={<Protected><ProfilePage/></Protected>}/><Route path="/settings/profile" element={<Protected><ProfileEditPage/></Protected>}/><Route path="/notifications" element={<Protected><NotificationsPage/></Protected>}/><Route path="/communities" element={<Protected><CommunitiesPage/></Protected>}/><Route path="/communities/new" element={<Protected><CommunityCreatePage/></Protected>}/><Route path="/communities/:slug/manage" element={<Protected><CommunityManagePage/></Protected>}/><Route path="/communities/:slug" element={<Protected><CommunityPage/></Protected>}/><Route path="*" element={<Navigate to="/feed" replace/>}/></Routes></AuthProvider>; }